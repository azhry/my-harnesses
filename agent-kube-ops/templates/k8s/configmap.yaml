apiVersion: v1
kind: ConfigMap
metadata:
  name: __APP_NAME__-config
  namespace: __NAMESPACE__
data:
  # Agent must populate these from app code and infrastructure reference
  APP_ENV: "__APP_ENV__"
  DB_HOST: "__DB_HOST__"
  DB_PORT: "__DB_PORT__"
  S3_BUCKET: "__S3_BUCKET__"
