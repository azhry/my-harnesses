# Frontend Test Plan

Delivery: `<DELIVERY_ID>` - `<TITLE>`

## Scope

- 

## Test Cases

| ID | Covers | Scenario | Method | Expected Result |
| --- | --- | --- | --- | --- |
| FETC-001 | AC-001 |  |  |  |

## Commands

Use bounded task-scoped commands. Prefer one spec/test target for this task.
On timeout, hang, or first failing run, record failed evidence and return to dev.
For local browser E2E, use visible/headed mode by default so the user can watch:
`cypress open`, `cypress run --headed --browser chrome --spec <spec>`, or
Playwright headed mode. Headless is only for CI, explicit user request, or a
final artifact-only check.

```bash

```

## Manual Checks

- 

## Results

| Command / Check | Status | Evidence |
| --- | --- | --- |
|  | pending |  |
